# Production Vite *fallback* when the hostname is unknown.
# Runtime hostname still wins:
#   direct.withtatva.ai                    → opsapi (prod PM)
#   tatva-direct-frontend-five.vercel.app  → devopsapi (dev PM)
VITE_PM_API_ENV=production
