# Deprecated — use backend integration

Voice commerce now runs **inside the Node backend** at `backend/voice/`.

See [backend/voice/README.md](../backend/voice/README.md).

You only need:

```bash
cd backend && npm run dev
cd frontend && npm run dev
```

Do **not** start this Python service unless you are experimenting with Whisper/Piper separately.
